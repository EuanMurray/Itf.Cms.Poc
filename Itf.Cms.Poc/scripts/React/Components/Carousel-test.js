import React from 'react';
import ReactDOM from 'react-dom';

var Carousel-test = props => (
    <div>{props.worked}</div>
);

var root = document.getElementById('carousel-test');

ReactDOM.render(
    <Carousel-test worked={data} />,
root
);